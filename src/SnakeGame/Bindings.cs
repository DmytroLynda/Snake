using Ninject.Modules;

namespace SnakeGame
{
    internal class Bindings : NinjectModule
    {
        public override void Load()
        {
            //TODO: Create the bindings for load the project and check it. After it, upload the project to GitHub for resume.
        }
    }
}
